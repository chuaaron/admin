spring:
  application:
    name: @project.artifactId@
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    joda-date-time-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  profiles:
    active: @spring.profiles.active@,mybatis
  session:
    store-type: none
info:
  app:
    version: 1.0.0